#!/bin/zsh

cd $PROJECT_DIR


if [[ $1 = '--sanitized' ]];then
    cmake -DENABLE_ASAN=ON -DENABLE_UBSAN=ON -B build-sanitized
    cd build-sanitized
    make
else
    cd cmake-build-debug-gnu
    cmake --build .
fi



